/**
 * @brief Represents the boundary of an element
 * @description This interface defines the position and size of an element's boundary.
 */
export interface Boundary {
  x: number;
  y: number;
  width: number;
  height: number;
}
