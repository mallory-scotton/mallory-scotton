/** Dependencies */
import { SVG } from '../utils';
import { PreferredTheme } from '../types';

export function footerlink2svg(name: string, theme: PreferredTheme) {
  // Create a new SVG canvas with specified dimensions
  const svg = new SVG(300, 113);

  // TODO: EMPTY ON PURPOSE

  return svg;
}
